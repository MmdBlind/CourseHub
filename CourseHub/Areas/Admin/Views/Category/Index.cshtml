@model List<CourseHub.Areas.Admin.Models.ViewModels.GetAllCategoriesViewModel>
<div class="card-body stat-box no-hover">
    <div class="table-admin-wrap mt-4">
        <table class="table-admin compact" dir="rtl">
            <thead>
                <tr>
                    <th>نام دسته</th>
                    <th>اسلاگ</th>
                    <th>دسته مرتبط</th>
                    <th>عملیات</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model as List<CourseHub.Areas.Admin.Models.ViewModels.GetAllCategoriesViewModel>)
                {
                    
                }
            </tbody>
        </table>
    </div>
</div>